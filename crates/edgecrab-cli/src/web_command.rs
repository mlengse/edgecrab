//! `/web` — opens the single-screen configurator (see `web_setup_tui.rs`).

/// Accent color for web overlays (warm amber).
pub const WEB_ACCENT: ratatui::style::Color = ratatui::style::Color::Rgb(255, 185, 75);
